{
  "title": "Extract Website Company Data",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of URLs to crawl (one per domain)",
      "editor": "requestListSources",
      "prefill": [
        { "url": "https://example.com" }
      ]
    },
    "timeoutSecs": {
      "title": "Timeout (seconds)",
      "type": "integer",
      "description": "Request timeout in seconds",
      "default": 30,
      "minimum": 5,
      "maximum": 120
    },
    "usePlaywrightFallback": {
      "title": "Use Playwright Fallback",
      "type": "boolean",
      "description": "Use Playwright for dynamic pages if HTTP fails",
      "default": true
    },
    "includeCompany": {
      "title": "Include Company Profile",
      "type": "boolean",
      "description": "Extract company information",
      "default": true
    },
    "includeContacts": {
      "title": "Include Contacts",
      "type": "boolean",
      "description": "Extract emails and phones",
      "default": true
    },
    "includeSocials": {
      "title": "Include Social Profiles",
      "type": "boolean",
      "description": "Extract social media links",
      "default": true
    },
    "includeTeam": {
      "title": "Include Team",
      "type": "boolean",
      "description": "Extract team members",
      "default": true
    },
    "keyPaths": {
      "title": "Custom Key Paths",
      "type": "array",
      "description": "Override default key paths (contact, about, team, legal, privacy)",
      "editor": "stringList",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "useOpenAI": {
      "title": "Use OpenAI for Enhanced Extraction",
      "type": "boolean",
      "description": "Use OpenAI to analyze page content and improve extraction of company data, addresses, and team members. Requires OPENAI_API_KEY environment variable.",
      "default": false
    },
    "openAIModel": {
      "title": "OpenAI Model",
      "type": "string",
      "description": "OpenAI model to use (gpt-4o-mini recommended for cost efficiency)",
      "default": "gpt-4o-mini",
      "enum": ["gpt-4o-mini", "gpt-4o", "gpt-4-turbo"]
    }
  },
  "required": ["startUrls"]
}
